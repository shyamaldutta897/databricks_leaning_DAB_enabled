# This is a Declarative Automation Bundle definition for dbx_learning_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbx_learning_dab
  uuid: 0c63a5bb-5c4b-40c5-bdf3-79fde948cf46

include:
  # - resources/*.yml
  # - resources/jobs/*.yml
  # - resources/pipelines/*.yml
    - resources/**/*.yml

# artifacts:
#   python_artifact:
#     type: whl
#     build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: production
    default: true
    workspace:
      host: https://adb-7405611310235533.13.azuredatabricks.net
      root_path: /Workspace/Users/shyamal6661@outlook.com/repos/databricks_leaning_DAB_enabled
    variables:
      catalog: skd_personal_bronze
      schema: new_schema
  prod:
    mode: production
    workspace:
      host: https://adb-7405611310235533.13.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/shyamal6661@outlook.com to make sure we only have a single copy.
      root_path: /Workspace/Users/shyamal6661@outlook.com/repos/databricks_leaning_DAB_enabled
    variables:
      catalog: skd_personal_bronze
      schema: prod
    permissions:
      - user_name: shyamal6661@outlook.com
        level: CAN_MANAGE
